// Expo Router entry — the file-based router under app/ handles all screens.
// package.json "main" already points to "expo-router/entry", but this file is
// kept as a fallback for tooling that resolves the default index entry.
import "expo-router/entry";
