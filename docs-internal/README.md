# Documentación interna — no se publica

Estos documentos vivían en `docs/`, que es la carpeta que **GitHub Pages sirve
públicamente**. Eso dejaba la auditoría de seguridad, el roadmap, la deuda
técnica y la auditoría de 64 hallazgos legibles por cualquiera en el dominio
del producto — verificado en vivo antes de moverlos.

`docs/` es el sitio público (landing + política de privacidad). Cualquier
documento nuevo de trabajo va acá.
